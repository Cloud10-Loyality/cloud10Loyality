apiVersion: apps/v1
kind: Deployment
metadata:
  name: manager-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: manager-mongo
  template:
    metadata:
      labels:
        app: manager-mongo
    spec:
      containers:
        - name: manager-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: manager-mongo-srv
spec:
  selector:
    app: manager-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017