apiVersion: apps/v1
kind: Deployment
metadata:
  name: tier-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tier-mongo
  template:
    metadata:
      labels:
        app: tier-mongo
    spec:
      containers:
        - name: tier-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: tier-mongo-srv
spec:
  selector:
    app: tier-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017